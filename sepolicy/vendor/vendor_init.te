# init.qti.kernel.rc sched
allow vendor_init vendor_proc_kernel_sched:file rw_file_perms;

# Dalvik props
set_prop(vendor_init, dalvik_config_prop)

# Mot props
set_prop(vendor_init, vendor_mot_prop)

# Power props
set_prop(vendor_init, vendor_power_prop)

# Sku prop
set_prop(vendor_init, vendor_sku_prop)

# Bluetooth prop
set_prop(vendor_init, bluetooth_prop)

# Default prop
set_prop(vendor_init, default_prop)

# Camera prop
set_prop(vendor_init, vendor_camera_prop)

# Radio prop
set_prop(vendor_init, radio_prop)

# CND prop
set_prop(vendor_init, vendor_cnd_prop)
